# Wave 2 inbox DEPRECATED — superseded by new wave plan

This directory was written for the **old** wave taxonomy (Wave 2 = Architecture & Hybrid). The user has since issued a new 8-wave learning plan in which:

- Old Wave 2 `dots-update-groups` and `dots-singleton-patterns` → New Wave 1
- Old Wave 2 `dots-transform-patterns` → New Wave 5
- Old Wave 2 `dots-hybrid-bridge` → New Wave 7
- Old Wave 2 `dots-event-driven-ecs` → New Wave 6 (or 8)

The active inbox is at `workspace/dots-program/inboxes/wave-1/` (HelloCube + Basic ECS Lifecycle). Do not work this inbox.
